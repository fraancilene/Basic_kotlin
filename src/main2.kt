fun main(args : Array<String>){

    // declarando arrays
    var nomes = arrayOf("Francilene", "Pedro", "Nicholas");
    println(nomes[0])
    nomes[0] = "Renata"
    println(nomes[0])

    // não tem como adicionar valor ao array criado
    //nomes[3] = "Renata"

    var numeros = arrayOf(20, 30, 40)
    var Numeros2 = intArrayOf(50, 60, 70)
    println(numeros[2])

}